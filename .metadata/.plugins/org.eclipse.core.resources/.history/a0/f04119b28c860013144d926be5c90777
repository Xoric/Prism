package xoric.prism.creator.custom.model;


public class SpriteNames
{
	public static String getFilename(int spriteIndex, int variation, ViewAngle v, int index)
	{
		return a.toString().toLowerCase() + ".var" + variation + "." + v.toString().toLowerCase() + "." + index + ".png";
	}
}
